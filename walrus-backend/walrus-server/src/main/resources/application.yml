server:
  port: 8080

spring:
  application:
    name: walrus-server
  datasource:
    url: jdbc:mysql://140.143.147.69:3306/walrus_db?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: walrusadmin
    password: walrusadmin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

app:
  jwt:
    secret: your_jwt_secret_key_here_please_change_it_in_production 